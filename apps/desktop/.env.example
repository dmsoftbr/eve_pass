# Copy to `.env` and fill with your Supabase project's values.
# The desktop app reads these at build/dev time (Vite exposes VITE_-prefixed vars).
VITE_SUPABASE_URL=https://YOUR-REF.supabase.co
VITE_SUPABASE_ANON_KEY=YOUR-ANON-KEY
